{
  "name": "cart360",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n API,CLIENT -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "dotnet run --project server/src/Cart360.API",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
